import { CreatePost } from "@/components/feed/create-post";

// Pantalla de crear publicación (SPEC 05). Render de la tarjeta del compositor.
export default function CrearPublicacionPage() {
  return <CreatePost />;
}
